services:
  - type: web
    name: channel-notify-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python notify_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: WEBHOOK_HOST
        sync: false
      - key: WEBHOOK_SECRET
        sync: false
